name: Go-Build
on:
  push:
    branches:
      - "master"
jobs:
  build:
    strategy:
      matrix:
        os: [ubuntu-latest, macOS-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    name: Build
    steps:
      - uses: actions/setup-go@v6
        with:
          go-version: "1.25"
      - uses: actions/checkout@v5
      - run: go mod download
      - name: Build
        run: go build -v .
